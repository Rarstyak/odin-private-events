<h1><%= @user.username %> User Dashboard</h1>

<h2>Hosted Events</h2>
<div id="events">
  <ul>
    <% @userEvents.each do |event| %>
      <li>
        <%= event.title + ':' + event.date + ':' + event.location %>
      </li>
    <% end %>
  </ul>
</div>
